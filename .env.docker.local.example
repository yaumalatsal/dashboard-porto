# Local dev override — copy to .env.docker.local and fill in real values.
# On the VPS this file is generated by CI/CD from GitHub secrets.
# Get the token from the LMS: https://yourday.duckdns.org (monitoring API)
MONITOR_API_TOKEN=your_monitoring_api_token_here
